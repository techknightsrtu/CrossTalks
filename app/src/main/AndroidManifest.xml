<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techknightsrtu.crosstalks">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.chat.ChatActivity"></activity>
        <activity android:name=".activity.chat.OnlineUsersActivity" />
        <activity android:name=".activity.profile.ProfileActivity" />
        <activity android:name=".activity.chat.HomeActivity" />
        <activity android:name=".activity.NoAppAccessActivity" />
        <activity android:name=".activity.auth.ChooseCollegeActivity" />
        <activity android:name=".activity.auth.ChooseAvatarActivity" />
        <activity android:name=".activity.auth.SelectGenderActivity" />
        <activity
            android:name=".activity.auth.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>